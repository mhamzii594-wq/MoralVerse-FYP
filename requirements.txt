django>=4.2,<5.0
python-dotenv>=1.0
dj-database-url>=2.1.0
psycopg2-binary>=2.9.0

# AI/ML Libraries
openai>=1.40.0
google-generativeai>=0.8.0
google-genai>=1.0.0
groq>=0.9.0
# ElevenLabs is optional; on Windows it may hit MAX_PATH issues during install.
# If you enable long paths or move the project to a shorter folder, you can re-enable it.
# elevenlabs>=1.9.0

# Image Processing
opencv-python>=4.10.0
Pillow>=10.0.0
numpy>=1.24.0

# Video Processing
moviepy>=2.0.0
mutagen>=1.46.0

# Text-to-Speech (Optional - for offline TTS)
# TTS>=0.22.0  # Uncomment if you want Coqui TTS offline support

# HTTP Requests
requests>=2.31.0

# Transactional email (Resend via django-anymail — bypasses blocked SMTP ports)
django-anymail[resend]>=10.0

# Google OAuth
django-allauth==0.63.6
PyJWT>=2.8.0

# Background Tasks (Optional)
# Django-Q for async task processing (optional).
# Note: `django-q>=1.4.1` doesn't exist on PyPI; keep this optional to avoid install failures.
# If you want background workers, pin an existing version (example below) and ensure Python compatibility:
# django-q==1.3.9

# Database (SQLite is included with Python, but you can use PostgreSQL)
# psycopg2-binary>=2.9.0  # Uncomment for PostgreSQL support


